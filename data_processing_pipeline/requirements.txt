# BabelProcessorDb Pipeline Requirements
# Updated: March 2026
# Minimal dependencies for EPUB processing and embedding generation

# Core Python packages
psycopg2-binary==2.9.9
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2  # MAJOR: 4.9.3 → 5.x (mostly backward compatible, check deprecated tree iteration)

# EPUB processing
ebooklib==0.18
zipfile36==0.1.3

# Text processing
nltk==3.9.1
textstat==0.7.3

# Data handling
pandas==2.2.3
numpy==2.1.3  # MAJOR: 1.24.4 → 2.x (breaking: np.string_, np.bool, np.int deprecated)

# Utilities
python-dotenv==1.0.1
tqdm==4.66.5
jsonschema==4.23.0

# Logging and monitoring
structlog==24.4.0

# Health check server
flask==3.0.3  # MAJOR: 2.3.3 → 3.x (removed Python 3.8 support, check Flask.before_first_request)

# Vector operations (if needed locally)
scikit-learn==1.5.2

# MLX Inference (Apple Silicon only — ~2.5x faster than Ollama at ~230 tok/s)
# NOTE: These do NOT run inside Docker (Linux). For local Apple Silicon scripts only.
# Replaces Ollama for local embedding generation:
#   bge-m3        → mlx-community/bge-m3 via mlx-embeddings
#   nomic-embed-text → mlx-community/nomic-embed-text-v1.5 via mlx-embeddings
#   mxbai-embed-large → mlx-community/mxbai-embed-large-v1 via mlx-embeddings
#   llama3.2:3b   → mlx-community/Llama-3.2-3B-Instruct-4bit via mlx-lm
mlx-lm>=0.18.0
mlx-embeddings>=0.0.5
