name: 🔒 DEPRECATED - Use production-api-deployment.yml

# This workflow has been replaced by:
# - production-api-deployment.yml (main deployment pipeline)
# - security-validation.yml (focused security scanning)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for running deprecated workflow'
        required: true

jobs:
  deprecated-notice:
    name: ⚠️ Deprecated Workflow
    runs-on: ubuntu-latest
    
    steps:
    - name: ⚠️ Deprecation Notice
      run: |
        echo "⚠️ THIS WORKFLOW IS DEPRECATED"
        echo "============================="
        echo ""
        echo "This security CI/CD pipeline has been replaced by:"
        echo "🚀 production-api-deployment.yml - Main deployment pipeline"
        echo "🔒 security-validation.yml - Focused security scanning"
        echo ""
        echo "These new workflows provide:"
        echo "✅ Faster execution (no agent coordination fluff)"
        echo "✅ Real API deployment to production"
        echo "✅ Comprehensive API testing (93.2% success rate)"
        echo "✅ Container-based deployment"
        echo "✅ Website documentation updates"
        echo ""
        echo "Please use the new workflows instead."
        echo "Reason for running: ${{ github.event.inputs.reason }}"